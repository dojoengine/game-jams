import { ThemeParams } from '@telegram-apps/bridge';
/**
 * Parses incoming value as theme parameters.
 * @param value - value to parse.
 */
export declare function parseThemeParams(value: unknown): ThemeParams;
