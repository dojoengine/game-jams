import { AsyncOptions } from '@telegram-apps/bridge';
/**
 * Requests the actual viewport content safe area insets information.
 * @param options - request options.
 * @throws {TypedError} ERR_UNKNOWN_ENV
 * @throws {TypedError} ERR_NOT_INITIALIZED
 * @throws {TypedError} ERR_NOT_SUPPORTED
 * @since Mini Apps v8.0
 * @example
 * if (requestContentSafeAreaInsets.isAvailable()) {
 *   const insets = await requestContentSafeAreaInsets();
 * }
 */
export declare const requestContentSafeAreaInsets: import('../../../../toolkit/wrapSafe.js').SafeWrapped<(options?: AsyncOptions) => import('@telegram-apps/bridge').CancelablePromise<import('@telegram-apps/bridge').SafeAreaInsets>, true, never>;
