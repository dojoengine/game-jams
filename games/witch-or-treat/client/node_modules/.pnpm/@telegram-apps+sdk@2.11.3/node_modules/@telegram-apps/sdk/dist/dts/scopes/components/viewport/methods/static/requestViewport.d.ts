import { CancelablePromise, AsyncOptions } from '@telegram-apps/bridge';
export interface RequestViewportResult {
    height: number;
    isExpanded: boolean;
    isStable: boolean;
    width: number;
}
/**
 * Requests viewport actual information from the Telegram application.
 * @param options - request options.
 * @example
 * if (requestViewport.isAvailable()) {
 *   const viewport = await requestViewport();
 * }
 */
export declare function requestViewport(options?: AsyncOptions): CancelablePromise<RequestViewportResult>;
